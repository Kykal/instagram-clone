//Typings
type DesktopNavBar = {
	activeItem: string;
	setItem: (item: string) => void;
}


//Main component content
const DesktopNavBar = (props: DesktopNavBar): JSX.Element => {


	//Main component render
	return (
		<>
			
		</>
	);
};


export default DesktopNavBar; //Export main component
